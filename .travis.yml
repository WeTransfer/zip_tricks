rvm:
- 2.1
- 2.2
- jruby-9.0
sudo: false
cache: bundler
matrix:
  allow_failures:
    - rvm: jruby-9.0
script: bundle exec rspec --tag ~long # Skip tests that generate large ZIPs or that take a very long time
